# 마비오라 (MabiAura) 사용 가이드

마비노기 화면을 **캡처만** 해서 동작하는 오버레이입니다. 와우의 WeakAuras 처럼 버프·디버프 상태를 화면에 띄웁니다. 게임 파일이나 메모리를 건드리지 않습니다.

- 상태창의 버프를 읽어 **꺼짐 / 켜라 / N초 미만 / 연장됨** 알림
- 버프 행·스킬 슬롯을 화면 원하는 곳에 **확대 표시**
- 보스 체력바 위 디버프 띠를 읽어 **빠진 디버프 목록**과 **버스트 알림**

## 0. 전제 조건 (안 맞으면 동작하지 않습니다)

| 항목 | 값 |
|---|---|
| 해상도 | 4K (3840×2160) |
| 창 모드 | 창모드 전체화면 |
| UI 배율 | 100% |
| 감시할 버프 | 상태창에서 **고정(핀)** 해둘 것. 고정된 버프만 감시합니다 |
| OS | Windows 10/11 |

오버레이는 스크린샷·OBS 창 캡처에 찍히지 않습니다 (일반 설정에서 바꿀 수 있음, §7).

## 1. 설치 / 실행

```
git clone <repo>
cd skilltrack
python -m venv .venv && .venv\Scripts\activate
pip install -r requirements.txt
python win\run.py
```

실행하면 홈 창이 뜨고, 닫으면 트레이(작업 표시줄 오른쪽 아래)에 남습니다.
트레이 아이콘: **왼쪽 클릭 = 켜기/끄기**, 더블클릭 = 홈, 오른쪽 클릭 = 메뉴.

## 2. 첫 설정 (캐릭터당 한 번)

1. **홈 → + 캐릭터 추가** — 이름은 아무거나
2. **영역 설정** — 상태창을 드래그해서 지정. 팁:
   - 시간이 표시되는 버프를 하나 켜두고
   - **어두운 곳**에서 (밝은 배경에선 비활성 회색 글자를 못 잘라 일부 행이 판정 불가가 됩니다)
   - 상태창 패널 폭에 맞춰서 (넓게 잡으면 바깥 밝은 배경이 글자로 오인)
3. 전환하면 레이아웃(행 좌표)을 자동으로 잡습니다. 홈 카드에 `✓ 레이아웃 (N행)` 이 뜨면 성공
4. **감시 항목** — 행마다 이름을 붙이고 알림을 고릅니다
   - 꺼짐 알림: 켜져 있다 꺼지는 순간 1회
   - 유지 필수: 꺼진 채 N초 지나면 M초마다 반복 ("켜세요")
   - N초 미만: 남은 시간 임계값 (연장 상태일 때 별도 임계값 가능)
   - 미러: 이 행을 화면에 확대 표시
5. (선택) **스킬창 영역 추가 → 스킬 표시** — 확대해서 볼 슬롯 체크. 모드: 항상 / 쿨중 / 흐림
6. (선택) **보스 디버프** — §4
7. **배치 편집** — 게임 화면 위에서 알림창·미러·보스 목록을 드래그로 옮기고 휠로 크기 조절. Shift+드래그/휠 = 그룹. 저장 버튼으로 확정
8. 트레이 아이콘 클릭 또는 홈의 스위치로 **켜기**

## 3. 평소 사용

- 켜면 감시·알림·오버레이 전부, 끄면 트레이만. 던전 들어갈 때 켜고 나오면 끄는 식으로
- 게임 창이 뒤로 가면 오버레이가 숨고 인식도 쉽니다 (시간은 계속 흐름)
- 캐릭터 바꾸면 홈에서 **이 캐릭터로 전환** (자동 인식 없음)
- 상태창을 옮기거나 고정 목록을 바꿨으면 **레이아웃 다시**

## 4. 보스 디버프

보스 체력바 위 디버프 띠(아이콘 줄)를 읽습니다. 띠가 있는 보스에서만 동작하고, 구형 레이드처럼 띠가 없는 보스는 자동으로 무시됩니다.

**홈 → 보스 디버프**
- 아이콘마다 **이름** (모든 캐릭터 공용) — 같은 이름을 주면 한 항목으로 묶임
- **감시**: 이 디버프가 빠지면 목록에 표시
- **재표시(초)**: `60,40,20` 이면 남은 시간이 그 이하로 내려갈 때 목록에 다시 표시(노란색). 비우면 빠졌을 때만
- **버스트**: 걸리는 순간 알림창에 문구 (예: `붕파! 극딜!`). 감시 없이 버스트만 켜도 됨 — 지속 10초짜리에 적합
- 창 맨 위 스위치로 이 캐릭터에서 보스 디버프 기능 전체 on/off

**동작**
- 목록은 바가 보이는 동안만 표시. 전부 걸려 있으면 아무것도 안 보임
- 보스마다 첫 전투에서는 첫 디버프가 걸린 뒤부터 동작하고, 그 보스를 기억해 두면 다음부터는 바가 뜨는 즉시 동작합니다
- 분 단위(`4M`)로 표시되는 동안은 초를 알 수 없으므로 재표시는 초 단위로 바뀐 뒤(60초 이하)부터

## 5. 알림음

일반 설정에서 소리 켜기 / wav 파일 지정. 기본은 무음.

## 6. 문제 해결

| 증상 | 확인 |
|---|---|
| 홈에 "상태창 항목이 변경됨" | 고정 목록을 바꿨거나 다른 캐릭터. 맞는 캐릭터로 전환하거나 **레이아웃 다시** |
| "밝은 곳에서 잡혀 일부 행 판정 못 함" | 어두운 곳에서 **레이아웃 다시** |
| 게임 창을 못 찾음 | 일반 설정의 창 제목이 게임 창 제목과 같은지 |
| 알림이 늦거나 시간이 튐 | 이펙트가 글자를 덮는 순간은 "모름"으로 두고 추정 타이머가 메웁니다. 자주 그러면 진단 프레임(§7)을 보내주세요 |
| 보스 디버프가 안 뜸 | 띠가 없는 보스거나, 그 보스 첫 전투(첫 디버프부터 동작). 로그에 `띠 패널 없음` 이면 정상 |
| 백신이 exe를 막음 | PyInstaller 로 만든 파이썬 앱의 흔한 오탐. 예외 등록 |
| 갑자기 꺼짐·이상 동작 | exe 옆 `mabiaura.log` 를 보내주세요 |

## 7. 진단 / 개발자에게 보내기

- 일반 설정 **"문제 진단용 프레임 자동 저장"** 을 켜두면 이상한 순간의 화면 조각이 `tests/fixtures/auto/`(상태창), `tests/fixtures/boss/auto/`(보스)에 저장됩니다. 그 png와 콘솔 로그를 같이 보내주세요
- 스크린샷에 오버레이가 보이게 하려면 일반 설정 **"오버레이를 스크린샷에 포함"**. 단, 오버레이가 상태창·스킬창·보스 바와 겹치면 자기 그림을 읽으니 겹치지 않게 배치할 것. OBS 는 **디스플레이 캡처** 소스로
- 콘솔 감시: `python win\watch.py --debug --boss` — 인식 결과를 표로 보여주고 `s`/`b` 로 프레임 저장
- 게임 스크린샷 기능으로 찍은 파일은 글꼴이 실제 화면과 달라 진단에 못 씁니다. 위 방법으로 저장한 것만 유효
