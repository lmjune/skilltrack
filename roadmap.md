# skilltrack 로드맵 (2026-09-16 기준)

## 완료

### 격자 검출 (`core/grid.py`)
- 드래그 영역 → 슬롯 좌표 리스트. 그룹 격자(4칸씩 띄어짐) 지원
- 부호 그래디언트 + 자기상관으로 pitch, 검은 테두리 쌍 검증으로 스냅
- 픽스처: 스크린샷 + 라이브 캡처

### 스킬 미러 오버레이 (`win/overlay.py`)
- 투명·클릭 통과·항상 위·캡처 제외 창. 선택 슬롯 실시간 확대 표시
- 라이브 동작 확인

### 상태창 파이프라인 (`core/rows.py`, `strokes.py`, `status.py`, `digits.py`, `pixelwatch.py`, `tracker.py`)
- 행 검출: 아이콘 프레임 열(끊어진 세로 선분이 행마다 반복) + 텍스트 엣지 + 간격 규칙. 배경 무관
- 고정 섹션만 감시 (유저가 게임에서 고정한 버프). 첫 섹션 = 간격이 벌어지기 전까지
- 활성/비활성: 캘리브레이션 때 저장한 획 자리 픽셀이 255(활성) / 209 또는 127(비활성) / 그 외(모름)
- 시간: 정확히 255 또는 (255,0,0)인 작은 덩어리 → 글자 매칭 (0~9, 분, 초 완성, 오차 1px). 오른쪽 정렬 검증
- 연장 변형: 활성 행의 이름 폭이 기준보다 10px 이상 길면 (예: 전장의 서곡 → 전장의 서곡(투안의 노래)) → 별도 임계값
- 추적기: 디바운싱 3프레임, 모름 프레임 무시, 추정 타이머(인식 끊겨도 흐름, 늘어나면 재동기화), 쿨다운
- 온오프 알림 두 모드: `꺼짐 알림`(켜져 있다 꺼지는 순간 1회), `유지 필수`(꺼진 채 유예 N초 지나면 M초마다 반복)
  - 마나실드·반신화: 둘 다 / 엘레멘탈 부여: 유지 필수 (부활 후 꺼진 채 남음)
  - 유지 필수는 전투 밖에서도 울림 → 2차 보스 체력바(전투 중 신호) 생기면 "전투 중에만" 옵션
- 레이아웃 저장/복원 (`profiles/status_layout.json`): 검출은 시작 시 1회, 실행 중엔 저장 좌표에서 읽기만
- 콘솔 감시 (`win/watch.py`): 상태표, 이벤트, 디버그 창, 의심 프레임 자동 저장
- 픽스처 10장 (어두움·눈밭·나무집×2·이펙트·안개·돌·돌+빨강·마을), 테스트 63개

### 확정된 게임 그리기 규칙 (코드 판단 근거)
| 항목 | 규칙 |
|---|---|
| 활성 글자 | 불투명 흰색, 정확히 255 |
| 비활성 글자 | 회색 단색 209 (던전 등 일부 상황 127). 배경 무관 |
| 1분 미만 시간 | 정확히 (255,0,0). 이펙트 빨강은 G,B ≥ 4 |
| 글자 외곽선 | 배경에 따라 있기도 없기도 함 → 조건으로 쓰지 않음 |
| 패널 | 반투명. 틴트 때문에 배경은 255가 될 수 없음 → 255 = 글자 |
| 이펙트 | 대부분 패널 뒤. 드물게 위(빛줄기)에 그려져 글자를 덮음 → 그 프레임은 모름, 타이머로 메움 |
| 렌더링 | 픽셀 단위로 동일. 같은 글자·아이콘은 프레임이 달라도 완전 일치 |

### 운영 전제
- 창모드, UI 배율 100%, 상태창 투명도 조절 불가
- 감시 대상은 게임에서 고정(핀)할 것. 고정 섹션은 안 움직임
- 캘리브레이션은 어두운 곳에서 1회. 캡처 영역은 패널 폭에 맞출 것 (넓으면 밖의 밝은 배경이 글자로 오인)
- 상태창 옮기거나 고정 목록 바꾸면 `--recalib`

---

## 남은 것 (1차 목표)

### 1. 알림 오버레이 ← 다음
- 이벤트(꺼짐/켜짐/N초 미만/연장) → 화면 텍스트 팝업 + 소리
- 몇 초 후 자동 소멸, 같은 항목 반복 제한
- `MirrorOverlay` 확장

### 2. 상태 미러 오버레이
- 선택 버프의 아이콘/이름/시간 이미지를 원하는 위치에 확대 표시
- 표시만 / 알림만 / 둘 다 조합 가능

### 3. 설정 UI (가장 양이 많음)
- 영역 드래그: 스킬창(여러 개), 상태창. 하드코딩 rect 대체. 드래그 중 실시간 검출 표시
- 감시 항목: 고정 섹션 행 썸네일 + 체크. 항목별 알림 설정 (초 임계값, 연장 시 임계값, 꺼짐/켜짐, 유지 필수+유예+간격, 소리)
- 이름 라벨 (선택 입력. 없으면 "행N")
- 슬롯별 표시 모드 (쿨 중에만 / 항상 / 항상+준비시 흐림)
- 진단 프레임 저장 on/off (기본 off, 기본 저장 종류: unknown·unknownstate·timelost·lost)

### 4. 스킬 오버레이 커스터마이징
- 편집 모드(게임 위에서 직접): 위치 드래그, 크기, 투명도, 그룹 단위 배치

### 5. 실행 환경
- 트레이 아이콘, 전역 단축키(F9 오버레이 토글, F10 설정)
- 게임 비활성 시 오버레이 숨김
- 설정 파일 저장/불러오기/내보내기

### 6. 배포
- PyInstaller exe (Windows에서 빌드, GitHub Actions 고려)
- 첫 실행 안내: 창모드·고정 목록·어두운 곳 캘리브레이션·백신 오탐

---

## 2차 (1차 완료 후)

### 7. 체력/마나/스태미나
- 하단 고정 위치, 세 막대의 채워진 길이 → 비율. 색이 확실해 배경 무관
- 알림: N% 미만. 표시: 막대 미러 또는 퍼센트
- 숫자(7327/8812)는 별도 폰트 → 필요 시 글자 학습

### 8. 보스 디버프 아이콘
- 보스 체력바 위 아이콘 띠. 한 줄, 왼쪽 정렬, 일정 간격, 폭 상한 = 체력바
- 아이콘 아래 글자: `3M` = 분, 숫자만 = 초, 빈 칸 = 시간 없음
- 준비: 전투 원본 캡처 수십 장 → 스크립트로 아이콘 전부 추출·중복 제거 → 종류별 이름 라벨링 → `assets/boss_icons/` + `boss_icons.json` (배포에 포함)
- 식별: 칸마다 라이브러리와 정확 매칭. 모르는 아이콘은 자동 저장
- 이벤트: 등록 아이콘 나타남(궁극기 디버프 등) / 사라짐 / N초 미만. 체크 목록 중 빠진 것 표시
- 시간 글자는 작은 폰트 → 별도 글자 라이브러리

### 미뤄둔 것
- 스킬 쿨타임 밝기 판정 ("쿨 중에만 표시" 모드에 필요)
- 단축키 기반 쿨타임 타이머 (스킬바에 없는 스킬)
- 물 위에서 나온 `0.00/0.45` 모름 케이스 (unknownstate 프레임 오면 확인)
- 픽스처 백업 (저장소 밖. 25MB+)
- `spec.md`에 위 규칙 반영

---

## 다음 세션 시작 절차
1. `git pull`, 픽스처 확인, `pytest tests/ -q`
2. `python win/watch.py --debug` 로 현재 동작 확인
3. 1번 알림 오버레이부터